# Dent-Html

Repository Engineer structure for dental HTML learning projects.

## Architecture

- `index.html` = production entry point
- `src/` = separated development components
- `assets/` = static resources
- `docs/` = technical documentation

## Development Rules

Every major component must have:
- clear ownership
- documentation
- debug notes
- change tracking
