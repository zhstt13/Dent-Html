// Dent HTML Quiz Controller Foundation

const quizController = {
  init() {
    console.log('Quiz controller initialized');
  },
  checkAnswer(question, answer) {
    return { question, answer };
  }
};

export default quizController;
